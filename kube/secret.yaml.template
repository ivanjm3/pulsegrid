# Template only — DO NOT commit a filled-in copy of this file.
# Copy to secret.yaml, fill in real values, apply, then delete the local copy
# (or manage via a secrets manager / External Secrets Operator instead).
#
# stringData lets you write plaintext here; Kubernetes base64-encodes it on
# apply, so no manual `base64` step is needed.
apiVersion: v1
kind: Secret
metadata:
  name: pulsegrid-db
  namespace: pulsegrid
type: Opaque
stringData:
  # postgres://<user>:<password>@<host>:5432/pulsegrid
  DB_DSN: "postgres://postgres:CHANGEME@postgres.pulsegrid.svc.cluster.local:5432/pulsegrid"
